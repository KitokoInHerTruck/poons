<center><%= form_with(model: @event.try(:persisted?) ? @event: Event.new, url: eadmin_events_path, locals: true) do |form| %> 
  <% if @event.try(:errors).try(:any?) %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@event.errors.count, "error") %> prohibited this event from
        being saved:
      </h2>
      <ul>
        <% @event.errors.full_message.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div>
      <%= form.label :Concours, style: "display: block" %>
      <%= form.text_field :title %>
    </div>

    <div>
      <%= form.label :description, style: "display: block" %>
      <%= form.text_area :description %>
    </div>

    <div>
      <%= form.label :Date, style: "display: block" %>
      <%= form.datetime_field :start_date %>
    </div>

    <div>
      <%= form.label :"Nombre de jour", style: "display: block" %>
      <%= form.number_field :duration %>
    </div>

    <div>
      <%= form.label :Prix, style: "display: block" %>
      <%= form.number_field :price %>
    </div>

    <div>
      <%= form.label :Lieu, style: "display: block" %>
      <%= form.text_field :location %>
    </div>

    <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
    </div>

    <div>
      <%= form.submit %>
    </div>

<% end %></center>